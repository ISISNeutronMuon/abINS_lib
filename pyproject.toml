[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "abinslib"
version = "0.1.0.post1"
description = "Inelastic neutron-scattering intensities from phonons."
long_description = """\
A library of calculation methods for inelastic neutron-scattering intensities from harmonic phonon simulation data.

This is intended to reproduce the calculations of the Abins algorithms in Mantid and make them more broadly available - eventually acting as a backend for that package.
It should also function as a playground for method development and benchmarking of different approximations in this area.

End users are primarily expected to interact with abinslib through downstream software: Mantid, workflow managers, alternative GUIs... The API is allowed to be a bit strict and dry compared to some scientific libraries, as the target audience are research software engineers.
"""
requires-python = ">=3.11"
dependencies = [
    "euphonic >= 2.0.0,<3",
    "resins == 0.1.0",
]

[project.optional-dependencies]
tutorial = ["pooch ~= 1.9"]

[dependency-groups]
test = [
    "pytest>=9.0,<10.0",
    "pytest-regressions[num]~=2.10.0",
    "pytest-cov~=7.0",
]
lint = ["ruff==0.15.11"]
doc = [
    "sphinx~=9.0.0",
    "furo==2025.12.19",
    "sphinx-autoapi~=3.8.0",
    "sphinx-gallery~=0.21.0",
    "myst-parser~=5.1.0",
    "matplotlib~=3.11.0",
    "pooch ~= 1.9",
]
dev = [
    {include-group = 'test'},
    {include-group = 'lint'},
]

[project.urls]
Repository = "https://github.com/isisneutronmuon/abINS_lib"
Documentation = "https://isisneutronmuon.github.io/abINS_lib"

[tool.coverage.paths]
source = ["src",
          "*/site-packages",
          ]

[tool.coverage.run]
source = ["src"]

[tool.setuptools.packages.find]
where =  ["src"]

[tool.setuptools.package-data]
abinslib = ["**/*.txt"]

[tool.pytest]
addopts = ["--import-mode=importlib"]
minversion = "9.0"
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
preview = true
explicit-preview-rules = true
select = [
       "D",
       "DOC",
       "F",
       "E",
       "UP",
       "COM",     # flake8-commas
       "PT",      # flake8-pytest-style
       "I",
       "C4",
       "A",
       "T10",
       "Q",
       "N",
]
ignore = [
       "COM812",
]

[tool.ruff.lint.isort]
force-sort-within-sections = true

[tool.ruff.lint.pep8-naming]
extend-ignore-names = [
    "kT",
]

[tool.ruff.lint.per-file-ignores]
# Ease docstring requirements for unit tests and workflows
"tests/*" = ["D", "DOC"]
"dev/validation/tests/*" = ["D", "DOC"]
"docs/source/tutorials/*" = [
    "D",
    "DOC",
    "E402",  # Allow python import closer to first use in tutorial
    ]

[tool.ruff.lint.pydocstyle]
convention = "google"
